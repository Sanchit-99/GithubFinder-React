import React from "react"

function About() {
  return (
    <div>
      <h1 className="text-5xl mb-5">Github Finder</h1>
      <p>
        This is a Web app built using React.js, It uses the official Github API
        to fetch the details of the users by their username and display in a
        custom build UI, which uses tailwind CSS and daisy UI.
      </p>
    </div>
  )
}

export default About
